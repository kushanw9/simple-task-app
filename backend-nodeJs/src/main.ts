import express from 'express';

const app = express();  // It creates a new Express application that represents backend web server.
const router = express.Router();
// use to define routes and handlers for specific paths or endpoints within the app.
                                 // They allow you to modularize and organize your route handling code.